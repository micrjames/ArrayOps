export class ArrayOps<T> {
   constructor(private A: Array<T>, private B: Array<T>) {}

   intersection(): T[] {
	  return Array<T>();
   }

   difference(): T[] {
	  return Array<T>();
   } 

   symmDifference(): T[] {
	  return Array<T>();
   }

   union(): T[] {
	  return Array<T>();
   }
}
